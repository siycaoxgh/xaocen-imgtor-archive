# v5 compatibility shims

These root-level forwarding modules were moved here after the package-only
source, worker, test and PyInstaller paths passed validation. They are retained
for historical rollback and are not part of the active runtime chain.
